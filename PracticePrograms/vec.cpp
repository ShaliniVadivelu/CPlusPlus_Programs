#include<iostream>
#include<vector>
using namespace std;

int main ()
{

   vector <int> vec;
   cout<<" "<<vec.size();
    
}

/* int n1,n2,num1,num2;
cin>>n1;
cin>>n2;

    vector <int> v1;
    for (int i=0;i<n1;i++)
    {
    cin>>num1;
    v1.push_back(num1);
    }

 vector <int> v2;
    for (int i=0;i<n2;i++)
    {
    cin>>num2;
    v2.push_back(num2);
    } 
    
    {                                                   // swapping
   v1.swap(v2);
   cout<<"\n after swapping of v1: ";
      for (int i=0;i<v1.size();i++)
   cout<<" "<<v1[i];

   cout<<"\n after swapping of v2: ";
      for (int i=0;i<v2.size();i++)
   cout<<" "<<v2[i];
    } 
} */